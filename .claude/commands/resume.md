Resume an in-progress task from `dev/active/`.

1. List all folders in `dev/active/` and let me pick one (or use the argument: $ARGUMENTS)
2. Read all three dev doc files: `*-plan.md`, `*-context.md`, `*-tasks.md`
3. Summarize: what's done, what's next, any blockers
4. Ask for confirmation before proceeding with the next phase