Start a new task with proper dev docs.

Task name: $ARGUMENTS

1. Create `dev/active/$ARGUMENTS/` directory
2. Create the three required dev doc files:
   - `$ARGUMENTS-plan.md` — with template structure from CLAUDE.md
   - `$ARGUMENTS-context.md` — with template structure from CLAUDE.md
   - `$ARGUMENTS-tasks.md` — with template structure from CLAUDE.md
3. Enter plan mode: research the codebase for relevant files, existing patterns, and dependencies
4. Draft the implementation plan and wait for approval before any code changes