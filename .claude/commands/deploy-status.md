Check the current deploy status:

1. Run `git log --oneline -3` to see what was recently pushed
2. Use Netlify MCP to check build status for the most recent deploy
3. If the deploy failed, fetch the build log and diagnose the error
4. Report: branch, commit, deploy status, and any errors